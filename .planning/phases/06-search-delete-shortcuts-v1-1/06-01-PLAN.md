---
phase: 06-search-delete-shortcuts-v1-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/search/search-index.ts
  - src/lib/search/search-index.test.ts
  - src/hooks/useSearchIndex.ts
  - src/components/chat-layout/SearchCommand.tsx
  - src/components/chat-layout/AppSidebar.tsx
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05]

must_haves:
  truths:
    - "User can press Cmd/Ctrl+K from anywhere to open search overlay"
    - "User can type a query and see matching messages with conversation titles"
    - "User can click a search result to navigate directly to that conversation"
    - "User can access search from sidebar (button above New Chat button)"
    - "Search shows minimal result format: conversation title + matching text snippet"
  artifacts:
    - path: "src/lib/search/search-index.ts"
      provides: "MiniSearch index wrapper with document indexing and search"
      min_lines: 40
    - path: "src/hooks/useSearchIndex.ts"
      provides: "React hook for managing search index lifecycle"
      exports: ["useSearchIndex"]
    - path: "src/components/chat-layout/SearchCommand.tsx"
      provides: "Search overlay component using CommandPalette pattern"
      contains: "Cmd/Ctrl+K"
  key_links:
    - from: "src/components/chat-layout/SearchCommand.tsx"
      to: "src/lib/search/search-index.ts"
      via: "useSearchIndex hook"
      pattern: "useSearchIndex"
    - from: "src/components/chat-layout/AppSidebar.tsx"
      to: "SearchCommand"
      via: "search button above New Chat"
      pattern: "Search|Cmd.*K"
---

<objective>
Add full-text search across all conversations using MiniSearch with command palette UX, enabling users to find messages by content or conversation title.

Purpose: Users can quickly find any past conversation or message without manually browsing the sidebar.
Output: Search index service, search hook, search command palette component, sidebar search button with keyboard hint.
</objective>

<execution_context>
@/Users/poak/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/poak/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-search-delete-shortcuts-v1-1/06-CONTEXT.md

# Reference existing patterns
@src/components/ui/command-palette.tsx
@src/lib/storage/conversations.ts
@src/components/chat-layout/AppSidebar.tsx
@src/components/chat-layout/app-sidebar-parts.tsx
@src/components/ui/kbd.tsx

# Key decisions from CONTEXT.md:
- UI Component: Use the same command palette component pattern (command-palette.tsx)
- Result format: Minimal — conversation title + matching text snippet (no date, no role)
- Result limit: Show 10 results initially, load more on scroll (infinite scroll pattern)
- Search scope: Conversation titles + message content (not memory facts)
- Click action: Just open the conversation (no scroll to message, no highlight)
- Multiple matches: Show separate result for each match in same conversation
- Post-click: Close search automatically after navigation
- Empty state: Helpful message with search tips (not just "No results")
- Debounce timing: Claude's discretion (suggest 300ms)
- Fuzzy search threshold: Claude's discretion (suggest 0.2)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install MiniSearch and create search index service</name>
  <files>
    package.json,
    src/lib/search/search-index.ts,
    src/lib/search/search-index.test.ts
  </files>
  <action>
Install minisearch package: `npm install minisearch`

Create `src/lib/search/search-index.ts`:
- Import MiniSearch from 'minisearch'
- Define SearchableDocument interface:
  ```ts
  interface SearchableDocument {
    id: string;                    // `${conversationId}:${messageId}`
    title: string;                 // Conversation title
    content: string;               // Message content
    conversationId: string;
    role: "user" | "assistant";
    updatedAt: number;
  }
  ```
- Create factory function `createSearchIndex()` that returns a new MiniSearch instance configured with:
  - fields: ["title", "content"]
  - storeFields: ["id", "title", "conversationId", "updatedAt"]
  - searchOptions: { fuzzy: 0.2, prefix: true, boost: { title: 2 } }
- Export `indexConversation(conversation: Conversation)` function to add all messages from a conversation to the index
- Export `indexConversations(conversations: Conversation[])` to bulk index
- Export `searchConversations(query: string, limit?: number)` function returning search results
- Export `removeConversationFromIndex(conversationId: string)` to remove conversation messages from index
- Handle edge cases: empty query returns empty results, index not initialized returns empty

Create `src/lib/search/search-index.test.ts`:
- Test indexing a conversation with multiple messages
- Test searching by message content
- Test searching by conversation title (should boost results)
- Test fuzzy matching
- Test removing conversation from index
- Test empty query handling

Do NOT:
- Include memory facts in search scope (per user decision)
- Use Web Workers for indexing (deferred optimization)
</files>
  <verify>
    cd /Users/poak/Documents/personal-project/Lokul && npm test -- --run src/lib/search/search-index.test.ts
  </verify>
  <done>
    - MiniSearch installed in package.json
    - search-index.ts exports createSearchIndex, indexConversation, indexConversations, searchConversations, removeConversationFromIndex
    - All tests pass
    - Search returns results ranked by title boost and fuzzy matching
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useSearchIndex hook for index lifecycle management</name>
  <files>src/hooks/useSearchIndex.ts</files>
  <action>
Create `src/hooks/useSearchIndex.ts`:
- Import useConversations from '@/hooks/useConversations'
- Import search index functions from '@/lib/search/search-index'
- Export `useSearchIndex()` hook that:
  - Lazy-initializes the search index on first use (not on app load)
  - Indexes all conversations when first accessed
  - Returns: { search, isIndexing, indexReady, reindex }
  - Debounce search calls internally (300ms) using setTimeout/clearTimeout pattern
  - Accepts AbortSignal for search cancellation
- Use useRef to store the MiniSearch instance (singleton pattern within hook)
- Handle reindexing when conversations change (optional - can be triggered manually)

Do NOT:
- Block the main thread during indexing (use chunked approach if >1000 messages)
- Auto-reindex on every conversation change (too expensive)
</files>
  <verify>
    cd /Users/poak/Documents/personal-project/Lokul && npm run typecheck
  </verify>
  <done>
    - useSearchIndex hook exported
    - Hook provides search, isIndexing, indexReady, reindex
    - Search is debounced at 300ms
    - Index is lazily initialized on first search
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SearchCommand component using CommandPalette pattern</name>
  <files>
    src/components/chat-layout/SearchCommand.tsx,
    src/components/chat-layout/AppSidebar.tsx,
    src/components/chat-layout/app-sidebar-parts.tsx
  </files>
  <action>
Create `src/components/chat-layout/SearchCommand.tsx`:
- Use the existing CommandPalette component pattern from `src/components/ui/command-palette.tsx`
- Props: { open: boolean, onOpenChange: (open: boolean) => void }
- Use useSearchIndex hook for search functionality
- Use useNavigate from react-router-dom for navigation
- Display results in CommandPalette groups:
  - Each result shows: conversation title (bold) + matching text snippet (truncated to ~80 chars)
  - Show separate result for each matching message in same conversation
  - Limit to 10 results initially (can extend with infinite scroll later)
- On result click:
  - Navigate to `/chat/${conversationId}`
  - Close search palette
- Empty state message: "No conversations found. Try different keywords or check your spelling."
- Loading state: Show skeleton or loading spinner while indexing/searching
- Include keyboard hint in sidebar using kbd.tsx component

Update `src/components/chat-layout/app-sidebar-parts.tsx`:
- Add SearchButton component with keyboard shortcut hint (⌘K)
- Place above NewChatItem
- Use Kbd component from @/components/ui/kbd for shortcut display

Update `src/components/chat-layout/AppSidebar.tsx`:
- Add SearchCommand component with controlled open state
- Wire SearchButton to open search on click
- Pass onOpenChange handler to manage search state

Do NOT:
- Highlight matching text in results (deferred)
- Scroll to specific message after navigation (per user decision)
- Include date or role in result format (per user decision)
</files>
  <verify>
    cd /Users/poak/Documents/personal-project/Lokul && npm run typecheck && npm run lint
  </verify>
  <done>
    - SearchCommand component renders CommandPalette with search results
    - Results show conversation title + text snippet
    - Click navigates to /chat/:id and closes search
    - Search button in sidebar shows ⌘K hint
    - Empty state shows helpful message
  </done>
</task>

</tasks>

<verification>
1. Run `npm test -- --run src/lib/search/search-index.test.ts` - all tests pass
2. Run `npm run typecheck` - no type errors
3. Run `npm run lint` - no lint errors
4. Manual verification:
   - Press Cmd/Ctrl+K - search palette opens
   - Type a query - results appear with title + snippet
   - Click a result - navigates to conversation, search closes
   - Click search button in sidebar - search palette opens
</verification>

<success_criteria>
- User can open search via Cmd/Ctrl+K keyboard shortcut
- User can search across all message content in all conversations
- Search results show matching text with conversation title and context
- User can click a search result to navigate to that conversation
- Search input is accessible from sidebar (above New Chat button)
</success_criteria>

<output>
After completion, create `.planning/phases/06-search-delete-shortcuts-v1-1/06-01-SUMMARY.md`
</output>
