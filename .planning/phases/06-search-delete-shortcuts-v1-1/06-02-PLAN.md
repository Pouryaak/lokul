---
phase: 06-search-delete-shortcuts-v1-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chat-layout/app-sidebar-parts.tsx
  - src/components/chat-layout/ConversationItemMenu.tsx
  - src/components/ui/confirm-dialog.tsx
  - src/components/chat-layout/AppSidebar.tsx
  - src/hooks/useConversationActions.ts
autonomous: true
requirements: [DEL-01, DEL-02, DEL-03, DEL-04]

must_haves:
  truths:
    - "Each conversation in sidebar has a 3-dot menu (shadcn DropdownMenu)"
    - "User can click Delete from the 3-dot menu"
    - "Delete action shows confirmation dialog with chat title before removing"
    - "Confirmed delete removes conversation from IndexedDB and sidebar"
    - "Delete shows toast notification 'Conversation deleted'"
    - "Post-delete navigation goes to /chat (new conversation)"
  artifacts:
    - path: "src/components/chat-layout/ConversationItemMenu.tsx"
      provides: "3-dot dropdown menu with Delete, Rename, Export options"
      contains: "DropdownMenu"
    - path: "src/components/ui/confirm-dialog.tsx"
      provides: "Reusable confirmation dialog component"
      contains: "Dialog"
    - path: "src/hooks/useConversationActions.ts"
      provides: "Hook for delete, rename, export actions with toast feedback"
      exports: ["useConversationActions"]
  key_links:
    - from: "src/components/chat-layout/ConversationItemMenu.tsx"
      to: "src/lib/storage/conversations.ts"
      via: "deleteConversation"
      pattern: "deleteConversation"
    - from: "ConversationItemMenu"
      to: "src/components/ui/confirm-dialog.tsx"
      via: "confirm dialog trigger"
      pattern: "ConfirmDialog"
</must_haves>

<objective>
Add 3-dot menu to each conversation in sidebar with Delete, Rename, and Export options. Delete shows confirmation dialog before removing from IndexedDB.

Purpose: Users can manage their conversations directly from the sidebar with destructive action protection.
Output: ConversationItemMenu component, ConfirmDialog component, useConversationActions hook.
</objective>

<execution_context>
@/Users/poak/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/poak/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-search-delete-shortcuts-v1-1/06-CONTEXT.md

# Reference existing patterns
@src/components/ui/dropdown-menu.tsx
@src/components/ui/dialog.tsx
@src/lib/storage/conversations.ts
@src/lib/storage/conversation-transfer.ts
@src/components/chat-layout/chat-topbar-menu.tsx
@src/components/chat-layout/app-sidebar-parts.tsx
@src/components/chat-layout/AppSidebar.tsx

# Key decisions from CONTEXT.md:
- Menu items: Delete, Rename, Export (include all export options: Markdown, JSON, Text)
- Confirmation dialog: Include chat title — "Delete '[Chat Title]'? This conversation will be permanently removed."
- Post-delete navigation: Navigate to /chat (new conversation)
- Feedback: Toast notification "Conversation deleted" (no undo option)
- Export menu should have parity with existing per-chat export options
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConfirmDialog component</name>
  <files>src/components/ui/confirm-dialog.tsx</files>
  <action>
Create `src/components/ui/confirm-dialog.tsx`:
- Use existing Dialog components from '@/components/ui/dialog'
- Create ConfirmDialogProps interface:
  ```ts
  interface ConfirmDialogProps {
    open: boolean;
    onOpenChange: (open: boolean) => void;
    title: string;
    description?: string;
    confirmLabel?: string;
    cancelLabel?: string;
    variant?: 'default' | 'destructive';
    onConfirm: () => void;
    onCancel?: () => void;
  }
  ```
- Render DialogContent with:
  - DialogTitle showing the title
  - DialogDescription showing description (optional)
  - DialogFooter with Cancel and Confirm buttons
  - Destructive variant: red confirm button
  - Default variant: primary confirm button
- Close dialog after confirm action
- Focus trap and keyboard handling (Escape cancels)

Do NOT:
- Add undo functionality (per user decision)
- Auto-close on outside click (require explicit action)
</files>
  <verify>
    cd /Users/poak/Documents/personal-project/Lokul && npm run typecheck
  </verify>
  <done>
    - ConfirmDialog component exported from '@/components/ui/confirm-dialog'
    - Shows title, description, cancel, and confirm buttons
    - Destructive variant shows red confirm button
    - Escape key cancels the dialog
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useConversationActions hook</name>
  <files>src/hooks/useConversationActions.ts</files>
  <action>
Create `src/hooks/useConversationActions.ts`:
- Import deleteConversation from '@/lib/storage/conversations'
- Import export functions from '@/lib/storage/conversation-transfer'
- Import useNavigate from react-router-dom
- Import toast from sonner
- Export useConversationActions() hook that returns:
  ```ts
  {
    deleteConversation: (id: string) => Promise<void>;
    renameConversation: (id: string, newTitle: string) => Promise<void>;
    exportAsMarkdown: (id: string) => Promise<void>;
    exportAsJson: (id: string) => Promise<void>;
    exportAsText: (id: string) => Promise<void>;
    isDeleting: boolean;
  }
  ```
- deleteConversation:
  - Calls deleteConversation(id) from storage
  - Shows toast "Conversation deleted"
  - Navigates to /chat (new conversation)
  - Throws on error (caller handles)
- renameConversation:
  - Uses updateConversationTitle from storage
  - Shows toast "Conversation renamed"
- exportAsMarkdown/Json/Text:
  - Reuse existing export functions from conversation-transfer.ts
  - Reuse download pattern from chat-topbar-menu.tsx
  - Show toast "Exported [format]"

Do NOT:
- Add undo functionality for delete
- Block UI during async operations
</files>
  <verify>
    cd /Users/poak/Documents/personal-project/Lokul && npm run typecheck
  </verify>
  <done>
    - useConversationActions hook exported
    - Provides delete, rename, export functions with toast feedback
    - Delete navigates to /chat after success
    - Export functions reuse existing transfer utilities
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ConversationItemMenu component</name>
  <files>src/components/chat-layout/ConversationItemMenu.tsx</files>
  <action>
Create `src/components/chat-layout/ConversationItemMenu.tsx`:
- Use DropdownMenu from '@/components/ui/dropdown-menu'
- Use ConfirmDialog from '@/components/ui/confirm-dialog'
- Use useConversationActions hook
- Props: { conversation: Conversation; onRename: () => void }
- Render 3-dot menu trigger button (MoreHorizontal icon, small, ghost variant)
- Menu items:
  - "Rename" - triggers inline rename or modal (call onRename callback)
  - DropdownMenuSeparator
  - "Export as Markdown" - calls exportAsMarkdown
  - "Export as JSON" - calls exportAsJson
  - "Export as Text" - calls exportAsText
  - DropdownMenuSeparator
  - "Delete" (destructive variant) - opens confirm dialog
- Confirm dialog:
  - Title: `Delete "${conversation.title}"?`
  - Description: "This conversation will be permanently removed."
  - Confirm button: "Delete" (destructive)
  - On confirm: call deleteConversation

Do NOT:
- Show Import option in sidebar menu (Import is in topbar only per existing pattern)
- Add undo option
</files>
  <verify>
    cd /Users/poak/Documents/personal-project/Lokul && npm run typecheck && npm run lint
  </verify>
  <done>
    - ConversationItemMenu component renders 3-dot menu
    - Menu shows Rename, Export options, Delete
    - Delete opens confirmation dialog with title
    - Confirm triggers deletion and shows toast
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate ConversationItemMenu into sidebar</name>
  <files>
    src/components/chat-layout/app-sidebar-parts.tsx,
    src/components/chat-layout/AppSidebar.tsx
  </files>
  <action>
Update `src/components/chat-layout/app-sidebar-parts.tsx`:
- Import ConversationItemMenu
- Update ConversationItemProps to include onRename callback
- Update ConversationItem component:
  - Show ConversationItemMenu on hover/focus (not always visible)
  - Add state for isRenaming
  - When isRenaming, show inline text input for editing title
  - On blur/enter, save new title and exit rename mode
  - Only show menu when not collapsed
- Position menu on right side of conversation item

Update `src/components/chat-layout/AppSidebar.tsx`:
- Pass onRename handler to ConversationSection
- Wire rename action to useConversationActions

Do NOT:
- Show menu when sidebar is collapsed (no space)
- Block conversation selection while menu is open
</files>
  <verify>
    cd /Users/poak/Documents/personal-project/Lokul && npm run typecheck && npm run lint
  </verify>
  <done>
    - Conversation items show 3-dot menu on hover (expanded sidebar only)
    - Delete triggers confirmation dialog then removes from IndexedDB
    - Rename enables inline title editing
    - Export options work with toast feedback
    - Post-delete navigates to /chat
  </done>
</task>

</tasks>

<verification>
1. Run `npm run typecheck` - no type errors
2. Run `npm run lint` - no lint errors
3. Manual verification:
   - Hover over conversation item - 3-dot menu appears
   - Click menu - shows Rename, Export options, Delete
   - Click Delete - confirmation dialog shows with title
   - Confirm delete - conversation removed, toast shown, navigated to /chat
   - Click Rename - inline input appears for editing
   - Export options download files with correct format
</verification>

<success_criteria>
- Each conversation in sidebar has a 3-dot menu (shadcn DropdownMenu)
- User can click "Delete" from the 3-dot menu
- Delete action shows confirmation dialog before removing
- Confirmed delete removes conversation from IndexedDB and sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/06-search-delete-shortcuts-v1-1/06-02-SUMMARY.md`
</output>
