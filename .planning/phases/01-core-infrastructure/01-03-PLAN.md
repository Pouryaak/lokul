---
phase: 01-core-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Progress.tsx
  - src/components/ui/Card.tsx
  - src/components/landing/HeroSection.tsx
  - src/components/landing/ProblemSolutionSection.tsx
  - src/components/landing/DemoSection.tsx
  - src/components/landing/HowItWorksSection.tsx
  - src/components/landing/ComparisonSection.tsx
  - src/components/landing/UseCasesSection.tsx
  - src/components/landing/TechnicalTrustSection.tsx
  - src/components/landing/FAQSection.tsx
  - src/components/landing/TestimonialsSection.tsx
  - src/components/landing/FinalCTASection.tsx
  - src/components/landing/FooterSection.tsx
  - src/components/onboarding/LoadingScreen.tsx
  - src/App.tsx
autonomous: false
requirements:
  - FIRST-01
  - FIRST-02
  - FIRST-03
  - FIRST-04
  - FIRST-05
must_haves:
  truths:
    - "User sees 'Start Chatting' button within 2 seconds of landing"
    - "User receives clear error if WebGPU is not supported"
    - "User sees honest progress during Quick Mode download (percentage + estimated time)"
    - "User can send first message within 60 seconds of landing page load"
    - "Progress is cancelable"
    - "Landing page has all 11 sections from home_page.md design"
  artifacts:
    - path: "src/components/landing/HeroSection.tsx"
      provides: "Hero section with spark logo, headline, CTA button, trust indicators"
      min_lines: 80
    - path: "src/components/landing/ProblemSolutionSection.tsx"
      provides: "Split screen pain points vs solutions"
      min_lines: 100
    - path: "src/components/landing/DemoSection.tsx"
      provides: "Demo video/GIF section showing offline capability"
      min_lines: 60
    - path: "src/components/landing/HowItWorksSection.tsx"
      provides: "3-step cards (Visit, Choose AI, Start Chatting)"
      min_lines: 80
    - path: "src/components/landing/ComparisonSection.tsx"
      provides: "Lokul vs ChatGPT comparison table"
      min_lines: 80
    - path: "src/components/landing/UseCasesSection.tsx"
      provides: "Three use case cards (Privacy, Digital Nomad, Budget)"
      min_lines: 100
    - path: "src/components/landing/TechnicalTrustSection.tsx"
      provides: "Dark section with open source, zero network, architecture proof"
      min_lines: 80
    - path: "src/components/landing/FAQSection.tsx"
      provides: "Accordion FAQ with 6 questions"
      min_lines: 120
    - path: "src/components/landing/TestimonialsSection.tsx"
      provides: "Three testimonial cards with quotes"
      min_lines: 60
    - path: "src/components/landing/FinalCTASection.tsx"
      provides: "Orange gradient final CTA section"
      min_lines: 50
    - path: "src/components/landing/FooterSection.tsx"
      provides: "4-column footer with links and social"
      min_lines: 80
    - path: "src/components/onboarding/LoadingScreen.tsx"
      provides: "Model download progress overlay"
      min_lines: 60
    - path: "src/App.tsx"
      provides: "Main app with landing page and loading flow integration"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/landing/HeroSection.tsx"
      via: "Landing page composition"
    - from: "src/components/landing/HeroSection.tsx"
      to: "src/lib/performance/gpu-detection.ts"
      via: "WebGPU check"
    - from: "src/App.tsx"
      to: "src/components/onboarding/LoadingScreen.tsx"
      via: "Conditional rendering after CTA click"
---

<objective>
Build the comprehensive marketing landing page following home_page.md design exactly: 11 sections (Hero, Problem/Solution, Demo, How It Works, Comparison, Use Cases, Technical Trust, FAQ, Testimonials, Final CTA, Footer). Include WebGPU detection, "Start Chatting" CTA, and loading screen with download progress.

Purpose: This is the user's first impression and marketing conversion page. The full landing page educates users about privacy benefits while the 2-second CTA visibility and 60-second first-message targets ensure retention.
Output: Complete 11-section landing page, reusable UI components, loading screen, and App.tsx integration with flow control.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@home_page.md

Phase 1 Context (locked decisions):
- Landing: Full 11-section marketing page per home_page.md
- Hero: Warm cream background (#FFF8F0), animated spark character, ember glow CTA
- Headline: "Lokul" + "Your AI. Your browser. Your privacy."
- Primary CTA: "Start Chatting - Free" with orange gradient (#FF6B35 to #FFB84D) and ember glow
- Trust indicators: "No account needed ✓ Free forever ✓ Open source"
- Problem/Solution: Split screen with dark left (pain points with red X) vs light right (solutions with orange checkmarks)
- Demo section: Shows offline capability with WiFi on/off comparison
- How It Works: 3 cards (Visit, Choose AI, Start Chatting)
- Comparison: Table comparing Lokul vs ChatGPT
- Use Cases: 3 cards (Privacy-Conscious, Digital Nomad, Budget-Conscious)
- Technical Trust: Dark section (#1A1A1A) with open source proof
- FAQ: Accordion with 6 questions
- Testimonials: 3 quote cards with star ratings
- Final CTA: Orange gradient background, large white button
- Footer: 4 columns with links

WebGPU detection: Check `navigator.gpu` availability
Error message: "Your browser doesn't support WebGPU", "Lokul requires Chrome 120+ or Edge 120+"
Actionable: Direct links to download Chrome/Edge
Progress shown: Percentage (0-100%), loading step label, time estimate
Steps: "Downloading Phi-2 model...", "Compiling shaders...", "Ready!"
</context>

<tasks>

<task type="auto">
<name>Task 1: Create UI primitives (Button, Progress, Card)</name>
<files>src/components/ui/Button.tsx, src/components/ui/Progress.tsx, src/components/ui/Card.tsx</files>
<action>
Create reusable UI components following shadcn/ui patterns and home_page.md design:

1. src/components/ui/Button.tsx:
   - Use class-variance-authority (CVA) for variants
   - Variants:
     * primary: Orange gradient bg (from-[#FF6B35] to-[#FFB84D]), white text, ember glow shadow
     * secondary: White bg, orange text
     * outline: Border only, orange border
     * ghost: Transparent
   - Sizes: sm, md, lg, xl (xl for hero CTA)
   - Props: variant, size, disabled, loading, onClick, children, className
   - Support disabled and loading states
   - Add hover: lift up 2px, glow intensifies

2. src/components/ui/Progress.tsx:
   - Props: value (0-100), max (default 100), showLabel (boolean), size (sm | md | lg)
   - Horizontal progress bar with fill animation
   - Label showing "X%" when showLabel is true
   - Use orange gradient for fill
   - Smooth transition on value changes
   - For download: show file size progress (e.g., "1.8GB / 2.8GB")

3. src/components/ui/Card.tsx:
   - Simple card wrapper with:
     * White background
     * Subtle shadow (shadow-md)
     * Border radius (rounded-xl)
     * Padding (p-6 or p-8)
     * Optional orange accent on top
   - Props: children, className, accent (boolean)

Follow CLAUDE.md component guidelines (max 200 lines, single responsibility).
</action>
<verify>
grep -q "export function Button" src/components/ui/Button.tsx && \
grep -q "cva" src/components/ui/Button.tsx && \
grep -q "export function Progress" src/components/ui/Progress.tsx && \
grep -q "export function Card" src/components/ui/Card.tsx
</verify>
<done>Button, Progress, and Card components created with variants, gradients, and proper TypeScript props</done>
</task>

<task type="auto">
<name>Task 2: Create HeroSection component</name>
<files>src/components/landing/HeroSection.tsx</files>
<action>
Create the Hero section following home_page.md Section 1 exactly:

1. Visual Design:
   - Background: Warm cream (#FFF8F0) base with subtle gradient to light orange at bottom
   - Full viewport height (min-h-screen) with centered content
   - Optional: Animated ember particles (floating upward, subtle)

2. Layout (centered):
   - Logo: Spark character (use spark-logo.svg, w-24 h-24 or 120px)
     * Gentle pulsing glow animation using CSS keyframes
   - Headline: "Lokul"
     * Font: text-6xl (64px), font-bold, text-[#1A1A1A]
   - Subheadline: "Your AI. Your browser. Your privacy."
     * Font: text-3xl (32px), font-medium, text-gray-600
   - Description (two lines):
     * "ChatGPT-quality AI running 100% in your browser."
     * "No servers. No tracking. Works offline."
     * Font: text-xl (20px), text-[#1A1A1A], leading-relaxed

3. Primary CTA Button:
   - Text: "Start Chatting - Free"
   - Style: Orange gradient (from-[#FF6B35] to-[#FFB84D])
   - Text: White, font-semibold, text-lg (18px)
   - Padding: py-4 px-12 (16px 48px)
   - Border radius: rounded-xl (12px)
   - Box shadow: Ember glow (0 0 30px rgba(255, 107, 53, 0.4))
   - Hover: translateY(-2px), intensified glow
   - onClick: Call onStart prop

4. Trust Indicators (below button):
   - "✓ No account needed ✓ Free forever ✓ Open source"
   - Font: text-sm (14px), text-gray-500
   - Checkmarks in orange

5. Social Proof:
   - "⭐ 12,847 developers trust Lokul"
   - Small, subtle, gray text

6. Scroll indicator:
   - Animated down arrow (ChevronDown from lucide-react)
   - Orange color, bouncing gently

7. WebGPU Detection:
   - Check navigator.gpu on mount
   - If unsupported: Show error banner instead of CTA
   - Error: "Your browser doesn't support WebGPU" with Chrome/Edge download links

Props interface: { onStart: () => void }
</action>
<verify>
grep -q "export function HeroSection" src/components/landing/HeroSection.tsx && \
grep -q "Lokul" src/components/landing/HeroSection.tsx && \
grep -q "Start Chatting" src/components/landing/HeroSection.tsx && \
grep -q "onStart" src/components/landing/HeroSection.tsx && \
grep -q "navigator.gpu" src/components/landing/HeroSection.tsx
</verify>
<done>HeroSection created with spark logo, gradient CTA, trust indicators, and WebGPU detection</done>
</task>

<task type="auto">
<name>Task 3: Create ProblemSolutionSection component</name>
<files>src/components/landing/ProblemSolutionSection.tsx</files>
<action>
Create the Problem/Solution split screen section (home_page.md Section 2):

1. Background: White with subtle top border in light orange

2. Layout: Split screen - Left (pain) vs Right (solution)
   - Grid: grid-cols-1 md:grid-cols-2
   - Full width, equal height columns

3. Left Side - Dark Background (#1A1A1A):
   - Header: "Tired of This?"
     * Font: text-4xl (36px), font-bold, white
   - Pain Points (4 items, each with red X icon):
     * "❌ $20/month for ChatGPT" - "That's $240/year for conversations"
     * "❌ Your data trains their AI" - "Every prompt you write belongs to them"
     * "❌ Stops working offline" - "No WiFi? No AI. Simple as that."
     * "❌ Privacy is a 'promise'" - "Trust them with your most private thoughts?"
   - Each pain point:
     * Large red X icon (XCircle from lucide-react, text-red-500)
     * Bold headline (font-semibold, text-lg, white)
     * Gray subtext (text-sm, text-gray-400)
     * 24px spacing between each

4. Right Side - Light Background (Warm Cream #FFF8F0):
   - Header: "Try This Instead"
     * Font: text-4xl (36px), font-bold, charcoal
     * Orange underline (decoration-orange-500, decoration-4)
   - Solutions (4 items, each with orange checkmark):
     * "✓ $0/month. Forever." - "The models are free. The code is free."
     * "✓ Your data stays on YOUR device" - "Not encrypted. Just... never sent anywhere."
     * "✓ Works on airplanes" - "Download once. Use forever. Even offline."
     * "✓ Privacy by architecture" - "Open source. Audit the code yourself."
   - Each solution:
     * Orange checkmark (CheckCircle from lucide-react, text-orange-500)
     * Bold headline (font-semibold, text-lg)
     * Dark gray subtext (text-sm, text-gray-600)
     * 24px spacing between each

5. Bottom of section:
   - "[See It In Action ↓]" button
   - Text button, orange color, with down arrow
   - Smooth scroll to Demo section
</action>
<verify>
grep -q "export function ProblemSolutionSection" src/components/landing/ProblemSolutionSection.tsx && \
grep -q "Tired of This" src/components/landing/ProblemSolutionSection.tsx && \
grep -q "Try This Instead" src/components/landing/ProblemSolutionSection.tsx && \
grep -q "grid-cols-2" src/components/landing/ProblemSolutionSection.tsx
</verify>
<done>ProblemSolutionSection created with split screen pain points vs solutions</done>
</task>

<task type="auto">
<name>Task 4: Create DemoSection component</name>
<files>src/components/landing/DemoSection.tsx</files>
<action>
Create the Demo section (home_page.md Section 3):

1. Background:
   - Gradient from white to pale orange
   - Subtle hexagon pattern (optional, very light)

2. Header (centered):
   - "Don't Trust Us. Test Us."
     * Font: text-5xl (48px), font-bold, charcoal
   - Subheader:
     * "Watch Lokul work. Then disconnect your WiFi and watch it keep working."
     * Font: text-xl (20px), text-gray-500

3. Demo Visualization (large, centered):
   - Create a mock browser window frame:
     * Gray border, rounded corners
     * Fake browser chrome (3 dots in top left)
   - Inside: Show two states side by side or as tabs

4. Frame 1: WiFi Connected
   - Header bar: "⚡ Quick Mode | WiFi: Connected"
   - Chat content:
     * User: "Explain quantum computing"
     * AI: "Quantum computing uses..." (streaming in)

5. Frame 2: WiFi Disconnected
   - Header bar: "⚡ Quick Mode | WiFi: Disconnected"
   - WiFi icon crossed out animation
   - "WiFi OFF" badge
   - Chat continues:
     * User: "Now explain it to a 5yo"
     * AI: "Imagine a super fast..." (response streaming instantly)

6. Final message:
   - Big orange text: "That's the power of local AI."

7. Below demo:
   - [Try It Yourself] button
   * Large orange button (same style as hero)
   * Scrolls to final CTA or starts onboarding

Use placeholder visuals - actual demo video/GIF can be added later.
</action>
<verify>
grep -q "export function DemoSection" src/components/landing/DemoSection.tsx && \
grep -q "Don't Trust Us" src/components/landing/DemoSection.tsx && \
grep -q "WiFi" src/components/landing/DemoSection.tsx && \
grep -q "Try It Yourself" src/components/landing/DemoSection.tsx
</verify>
<done>DemoSection created with offline capability demonstration</done>
</task>

<task type="auto">
<name>Task 5: Create HowItWorksSection component</name>
<files>src/components/landing/HowItWorksSection.tsx</files>
<action>
Create the How It Works section (home_page.md Section 4):

1. Background: White, clean minimal

2. Header (centered):
   - "Three Steps to Private AI"
   - Font: text-4xl (42px), font-bold

3. Three Cards (horizontal layout):
   - Grid: grid-cols-1 md:grid-cols-3 gap-8
   - Max width container

Card 1: "1️⃣ Visit Lokul"
- Icon: Globe or Browser icon (orange)
- Orange accent on top (border-t-4 border-orange-500)
- Content:
  * "No download needed"
  * "No account required"
  * "Just open browser"
- Bottom: "Takes 5 seconds"

Card 2: "2️⃣ Choose Your AI"
- Icon: Download icon with progress bar visual
- Orange accent on top
- Content:
  * "Quick: 80MB, Fast"
  * "Smart: 2.8GB, Better"
  * "Genius: 6.4GB, Best"
- Bottom: "Download once, use forever"

Card 3: "3️⃣ Start Chatting"
- Icon: MessageCircle with sparkle
- Orange accent on top
- Content:
  * "Instant responses"
  * "Complete privacy"
  * "Works offline"
- Bottom: "That's it. Really."

4. Card design:
   - White background
   - Subtle shadow (shadow-lg)
   - Border radius (rounded-xl)
   - Padding (p-8)
   - Hover: lift up slightly

5. Below cards:
   - "The whole setup takes about 30 seconds. Then it's yours. Forever."
   - [Get Started] button (orange, large)
</action>
<verify>
grep -q "export function HowItWorksSection" src/components/landing/HowItWorksSection.tsx && \
grep -q "Three Steps" src/components/landing/HowItWorksSection.tsx && \
grep -q "grid-cols-3" src/components/landing/HowItWorksSection.tsx && \
grep -q "Visit Lokul" src/components/landing/HowItWorksSection.tsx
</verify>
<done>HowItWorksSection created with 3-step cards</done>
</task>

<task type="auto">
<name>Task 6: Create ComparisonSection component</name>
<files>src/components/landing/ComparisonSection.tsx</files>
<action>
Create the Comparison section (home_page.md Section 5):

1. Background: Very pale orange (#FFF8F0), full width

2. Header (centered):
   - "Lokul vs. ChatGPT"
   - Font: text-5xl (48px), font-bold
   - Subheader:
     * "We won't bad-mouth ChatGPT. It's great."
     * "But if you value privacy, here's what's different:"
     * Font: text-lg (18px), text-gray-500

3. Comparison Table:
   - Clean, modern table design
   - 3 columns: Feature | ChatGPT Plus | Lokul
   - 5 rows:

Row 1: Privacy
- ChatGPT: "❌ Cloud" / "Your data sent to OpenAI's servers"
- Lokul: "✅ 100% Local" / "Stays in your browser. Period." (highlighted)

Row 2: Offline
- ChatGPT: "❌ No" / "Needs internet"
- Lokul: "✅ Yes" / "Works on airplanes" (highlighted)

Row 3: Cost
- ChatGPT: "❌ $20/month" / "$240/year"
- Lokul: "✅ Free" / "$0. Forever." (highlighted)

Row 4: Speed
- ChatGPT: "⚠️ Network" / "Depends on connection"
- Lokul: "✅ Instant" / "Runs on your device (faster)" (highlighted)

Row 5: Open Source
- ChatGPT: "❌ No" / "Black box"
- Lokul: "✅ Yes" / "Audit it yourself" (highlighted)

4. Table styling:
   - Lokul column has subtle orange highlight (bg-orange-50)
   - Checkmarks/X icons
   - Hover on rows: slight highlight
   - Mobile: Stack vertically (responsive)

5. Below table:
   - "Both are good. Choose what matters to you."
   - Two buttons:
     * [Try Lokul Free] - Orange filled
     * [Learn More About ChatGPT →] - Outline only
</action>
<verify>
grep -q "export function ComparisonSection" src/components/landing/ComparisonSection.tsx && \
grep -q "Lokul vs" src/components/landing/ComparisonSection.tsx && \
grep -q "table" src/components/landing/ComparisonSection.tsx && \
grep -q "ChatGPT" src/components/landing/ComparisonSection.tsx
</verify>
<done>ComparisonSection created with Lokul vs ChatGPT table</done>
</task>

<task type="auto">
<name>Task 7: Create UseCasesSection component</name>
<files>src/components/landing/UseCasesSection.tsx</files>
<action>
Create the Use Cases section (home_page.md Section 6):

1. Background: White

2. Header (centered):
   - "Who Uses Lokul?"
   - Font: text-5xl (48px), font-bold

3. Three Large Cards (horizontal layout):
   - Grid: grid-cols-1 md:grid-cols-3 gap-8

Card 1: The Privacy-Conscious
- Icon: Shield or Lock (large, orange)
- Headline: "\"I don't trust cloud AI with my thoughts\""
- Body text:
  * "Writers, therapists, lawyers, and anyone working with sensitive information."
  * "Your journal entries, client notes, and private thoughts stay private."
  * "Because they never leave your device."
- Quote:
  * ""I use Lokul for therapy journaling. Finally, an AI I can actually trust.""
  * "- Sarah K., Clinical Psychologist"
  * Italic, gray, smaller text

Card 2: The Digital Nomad
- Icon: Plane or MapPin
- Headline: "\"I need AI that works anywhere\""
- Body text:
  * "Travelers, remote workers, and people with unreliable internet."
  * "Write blog posts on flights. Code at the beach. Plan trips in remote cafes."
  * "Download once at home. Use everywhere."
- Quote:
  * ""Wrote my entire newsletter on a 14-hour flight. Zero WiFi needed.""
  * "- Marcus T., Digital Nomad"

Card 3: The Budget-Conscious
- Icon: Wallet or DollarSign
- Headline: "\"I'm tired of $20/month subscriptions\""
- Body text:
  * "Students, freelancers, and anyone who thinks AI shouldn't require subscriptions."
  * "ChatGPT Plus: $240/year"
  * "Claude Pro: $240/year"
  * "Lokul: $0/year"
  * "Same quality. Zero cost."
- Quote:
  * ""Canceled my ChatGPT subscription. Haven't looked back.""
  * "- David L., Freelance Developer"

4. Card design:
   - Large padding (p-8)
   - Subtle border
   - Icon at top (48px, orange)
   - Headline in quotes, larger font
   - Body text with good line height
   - Quote at bottom with attribution
   - Hover: slight lift and glow

5. Below cards:
   - [See If Lokul Is For You] - Orange button
</action>
<verify>
grep -q "export function UseCasesSection" src/components/landing/UseCasesSection.tsx && \
grep -q "Who Uses Lokul" src/components/landing/UseCasesSection.tsx && \
grep -q "Privacy-Conscious" src/components/landing/UseCasesSection.tsx && \
grep -q "Digital Nomad" src/components/landing/UseCasesSection.tsx
</verify>
<done>UseCasesSection created with 3 persona cards</done>
</task>

<task type="auto">
<name>Task 8: Create TechnicalTrustSection component</name>
<files>src/components/landing/TechnicalTrustSection.tsx</files>
<action>
Create the Technical Trust section (home_page.md Section 7):

1. Background: Dark (#1A1A1A), code-like aesthetic

2. Header (centered, white):
   - ""How Do I Know It's Really Private?""
   - Font: text-4xl (42px), font-bold, white
   - Subheader: "Fair question. Here's the proof:"
   - Font: text-xl (20px), text-gray-400

3. Three Proof Points (horizontal cards):

Proof 1: Open Source
- Icon: GitBranch or Code (white/orange)
- Title: "✓ All Code is Public"
- Body:
  * "Every line of code is on GitHub."
  * "Audit it yourself. Fork it. Modify it."
  * "No hidden tracking. No secret servers."
  * "What you see is what you get."
- Button: "[View Source Code →]"
  * White text, orange border

Proof 2: Zero Network Activity
- Icon: WifiOff or Activity
- Title: "✓ Test It Yourself"
- Body:
  * "1. Open browser dev tools"
  * "2. Go to Network tab"
  * "3. Use Lokul"
  * "4. Watch: Zero requests sent"
  * "Your browser doesn't lie. If data was being sent, you'd see it."
- Button: "[See How to Test →]"

Proof 3: Architecture
- Icon: Cpu or Layers
- Title: "✓ Can't Send Data (By Design)"
- Body:
  * "There's no server to send data to."
  * "Everything runs client-side."
  * "WebGPU processes AI in your browser."
  * "IndexedDB stores conversations locally."
  * "It's not about trust. It's about math."
- Button: "[Read Technical Docs →]"

4. Bottom of section:
   - "Still skeptical? Good. That's the right attitude for privacy."
   - [Join Our Discord] - Ask us anything.
   - Outline button style
</action>
<verify>
grep -q "export function TechnicalTrustSection" src/components/landing/TechnicalTrustSection.tsx && \
grep -q "How Do I Know" src/components/landing/TechnicalTrustSection.tsx && \
grep -q "bg-[#1A1A1A]" src/components/landing/TechnicalTrustSection.tsx && \
grep -q "Open Source" src/components/landing/TechnicalTrustSection.tsx
</verify>
<done>TechnicalTrustSection created with dark theme and proof points</done>
</task>

<task type="auto">
<name>Task 9: Create FAQSection component</name>
<files>src/components/landing/FAQSection.tsx</files>
<action>
Create the FAQ section with accordion (home_page.md Section 8):

1. Background: Pale orange (#FFF8F0)

2. Header (centered):
   - "Questions?"
   - Font: text-5xl (48px), font-bold

3. FAQ Items (Accordion style):
   - Max-width container (max-w-3xl)
   - 6 FAQ items

Q1: "Is it really free? What's the catch?"
Expanded:
- "Yes. Actually free. No catch."
- "The AI models we use (Phi, Llama, Mistral) are open source and free to use."
- "The code is open source and free to use."
- "There's no server to pay for (because there is no server)."
- "We might add optional premium features later, but the core product will ALWAYS be free."
- "This isn't a free trial. It's just free."

Q2: "How good is it compared to ChatGPT?"
Expanded:
- "Honest answer: It depends."
- Lists Quick Mode, Smart Mode, Genius Mode with descriptions
- "The gap is closing fast."
- "Try both. Decide for yourself."

Q3: "Will this work on my computer?"
Expanded:
- "If you have: Chrome 113+ or Edge 113+, 4GB+ RAM, a graphics card"
- "Then yes, it'll work."
- Lists tested devices (MacBooks, Windows laptops, Linux)
- "Try Quick Mode first (works on almost anything)."

Q4: "Can you see my conversations?"
Expanded:
- "No. And we can prove it."
- Network tab test instructions
- "Your conversations never leave your browser."
- "We literally CAN'T see them."
- Open source challenge with $10,000 bounty

Q5: "What happens to my conversations if I clear my browser?"
Expanded:
- "They're deleted. Forever."
- "This is by design. Privacy means you're in control."
- Export options (Markdown, Text, JSON)

Q6: "Is this legal?"
Expanded:
- "Yes! 100% legal."
- Lists model licenses (MIT, Llama, Apache 2.0)
- "This isn't piracy. This is how open source is supposed to work."

4. Accordion behavior:
   - Click to expand/collapse
   - Smooth animation
   - Chevron icon rotates
   - Only one open at a time (optional)

5. Below FAQ:
   - "Still have questions?"
   - Three buttons: [Join Discord] [Email Us] [Read Docs]
   - Outlined in orange
</action>
<verify>
grep -q "export function FAQSection" src/components/landing/FAQSection.tsx && \
grep -q "Questions" src/components/landing/FAQSection.tsx && \
grep -q "free" src/components/landing/FAQSection.tsx && \
grep -q "accordion" src/components/landing/FAQSection.tsx || grep -q "FAQ" src/components/landing/FAQSection.tsx
</verify>
<done>FAQSection created with 6 accordion items</done>
</task>

<task type="auto">
<name>Task 10: Create TestimonialsSection component</name>
<files>src/components/landing/TestimonialsSection.tsx</files>
<action>
Create the Testimonials section (home_page.md Section 9):

1. Background: White

2. Header (centered):
   - "Don't Just Take Our Word For It"
   - Font: text-4xl (42px), font-bold

3. Three Testimonial Cards (horizontal layout):
   - Grid: grid-cols-1 md:grid-cols-3 gap-8

Testimonial 1:
- 5 orange stars (⭐⭐⭐⭐⭐)
- Quote: ""Finally, an AI I can trust with my therapy journal. It's not about hiding from OpenAI. It's about having a truly private space for my thoughts.""
- Attribution: "- Sarah K., Clinical Psychologist"

Testimonial 2:
- 5 orange stars
- Quote: ""Wrote an entire blog post on a 14-hour flight to Tokyo. Zero WiFi. Zero problems. This is the future.""
- Attribution: "- Marcus T., Digital Nomad & Writer"

Testimonial 3:
- 5 orange stars
- Quote: ""Canceled my ChatGPT Plus subscription. Saved $240/year. Lokul does everything I need. And it's FASTER.""
- Attribution: "- David L., Freelance Developer"

4. Card design:
   - Subtle shadow
   - Padding (p-8)
   - Border radius (rounded-xl)
   - Stars at top (orange color)
   - Quote in larger font, italic
   - Attribution at bottom, gray, smaller
   - Hover: slight lift and glow

5. Below testimonials:
   - "Join 12,847 developers who trust Lokul"
   - "⭐⭐⭐⭐⭐ Rated 4.9/5 on GitHub"
   - Live GitHub star count (optional mock for now)
</action>
<verify>
grep -q "export function TestimonialsSection" src/components/landing/TestimonialsSection.tsx && \
grep -q "Don't Just Take Our Word" src/components/landing/TestimonialsSection.tsx && \
grep -q "Sarah K" src/components/landing/TestimonialsSection.tsx && \
grep -q "⭐" src/components/landing/TestimonialsSection.tsx
</verify>
<done>TestimonialsSection created with 3 quote cards</done>
</task>

<task type="auto">
<name>Task 11: Create FinalCTASection component</name>
<files>src/components/landing/FinalCTASection.tsx</files>
<action>
Create the Final CTA section (home_page.md Section 10):

1. Background:
   - Gradient: Orange to amber (from-[#FF6B35] to-[#FFB84D])
   - Full width
   - High contrast

2. Center-aligned content:
   - Header (large, white):
     * "Ready to Own Your AI?"
     * Font: text-6xl (56px), font-bold, white
   - Subheader:
     * "No email. No credit card. No commitment."
     * "Just better AI. In 30 seconds."
     * Font: text-2xl (24px), white with slight transparency (text-white/80)

3. CTA Button (huge):
   - Text: "Start Chatting - Free"
   - Design:
     * White background
     * Orange text (text-orange-500)
     * Font: text-2xl (24px), font-bold
     * Padding: py-6 px-16 (24px 64px)
     * Border radius: rounded-2xl (16px)
     * Box shadow: 0 8px 32px rgba(0,0,0,0.2)
     * Hover: translateY(-4px), shadow intensifies
   - onClick: Call onStart prop

4. Below button:
   - "✓ Takes 30 seconds ✓ Works in your browser ✓ Free forever"
   - White text, small icons

5. Even below that:
   - "12,847 developers are already using Lokul."
   - "[See what they're saying →]"
   - White text, subtle, links to testimonials

Props interface: { onStart: () => void }
</action>
<verify>
grep -q "export function FinalCTASection" src/components/landing/FinalCTASection.tsx && \
grep -q "Ready to Own Your AI" src/components/landing/FinalCTASection.tsx && \
grep -q "Start Chatting" src/components/landing/FinalCTASection.tsx && \
grep -q "onStart" src/components/landing/FinalCTASection.tsx
</verify>
<done>FinalCTASection created with orange gradient and large white CTA</done>
</task>

<task type="auto">
<name>Task 12: Create FooterSection component</name>
<files>src/components/landing/FooterSection.tsx</files>
<action>
Create the Footer section (home_page.md Section 11):

1. Background: Dark charcoal (#1A1A1A)

2. Layout: Four Columns

Column 1: Brand
- Spark Logo (orange glow)
- "Lokul"
- "Your AI. Your browser. Your privacy."
- "ChatGPT-quality AI that runs 100% in your browser."
- "© 2026 Lokul"
- "Open source. MIT License."

Column 2: Product
- "Product"
- Links:
  * [Try Demo]
  * [How It Works]
  * [Pricing (Free!)]
  * [Roadmap]
  * [Changelog]

Column 3: Resources
- "Resources"
- Links:
  * [Documentation]
  * [FAQ]
  * [Blog]
  * [GitHub]
  * [Discord]

Column 4: Company
- "Company"
- Links:
  * [About]
  * [Privacy Policy]
  * [Terms of Service]
  * [Contact]
  * [Press Kit]

3. Social Links (bottom):
   - Icons: GitHub, Discord, Twitter, Reddit
   - Orange on hover
   - Horizontal row

4. Very bottom:
   - "Made with ❤️ for privacy"
   - "⭐ Star us on GitHub"
   - Centered, smaller text, gray

5. Styling:
   - Text colors: White for headers, gray-400 for links
   - Link hover: Orange color
   - Generous padding
   - Border-top: subtle gray
</action>
<verify>
grep -q "export function FooterSection" src/components/landing/FooterSection.tsx && \
grep -q "Product" src/components/landing/FooterSection.tsx && \
grep -q "Resources" src/components/landing/FooterSection.tsx && \
grep -q "MIT License" src/components/landing/FooterSection.tsx
</verify>
<done>FooterSection created with 4 columns and social links</done>
</task>

<task type="auto">
<name>Task 13: Create LoadingScreen component</name>
<files>src/components/onboarding/LoadingScreen.tsx</files>
<action>
Create the model loading progress overlay component:

1. Full-screen overlay:
   - Fixed position, covers entire viewport
   - Background: White with slight transparency or solid
   - Z-index high (z-50)

2. Centered content:
   - Logo: spark-logo.svg (larger, pulsing animation)
   - Title: "Downloading Quick Mode"
   - Progress bar with percentage

3. Progress display:
   - Progress bar (use Progress component)
   - Percentage text (e.g., "62%")
   - File size progress: "1.8GB / 2.8GB"
   - Time estimate: "⏱️ About 3 minutes remaining"

4. Step labels:
   - "Downloading Phi-2 model..."
   - "Compiling shaders..."
   - "Ready!"

5. Helpful message:
   - "💡 You can keep chatting. We'll switch automatically."

6. Cancel option:
   - [Chat in Quick Mode] button (if already available)
   - [Pause] or [Cancel] button
   - On cancel: return to landing

7. Completion state:
   - "✓ Smart Mode Ready!"
   - "Downloaded and cached. Now works completely offline."
   - [Start Using Smart Mode] button
   - Auto-dismiss after 3 seconds

Props interface: { onCancel: () => void, onReady: () => void, modelName?: string }
</action>
<verify>
grep -q "export function LoadingScreen" src/components/onboarding/LoadingScreen.tsx && \
grep -q "Progress" src/components/onboarding/LoadingScreen.tsx && \
grep -q "Downloading" src/components/onboarding/LoadingScreen.tsx && \
grep -q "onCancel" src/components/onboarding/LoadingScreen.tsx
</verify>
<done>LoadingScreen created with progress bar, time estimates, and cancel functionality</done>
</task>

<task type="auto">
<name>Task 14: Integrate landing page in App.tsx</name>
<files>src/App.tsx</files>
<action>
Update App.tsx to orchestrate the full landing page and onboarding flow:

1. Import all landing sections:
   - HeroSection
   - ProblemSolutionSection
   - DemoSection
   - HowItWorksSection
   - ComparisonSection
   - UseCasesSection
   - TechnicalTrustSection
   - FAQSection
   - TestimonialsSection
   - FinalCTASection
   - FooterSection

2. Import onboarding:
   - LoadingScreen
   - useSettingsStore
   - useModelStore
   - QUICK_MODEL

3. State management:
   - useState for appState: "landing" | "loading" | "chat" | "error"
   - useEffect to check if user has completed setup (from settings store)

4. Flow logic:
   - On mount: Check settings.hasCompletedSetup
     * If true: Skip to "chat" state
     * If false: Show "landing" state

   - landing state:
     * Render all landing page sections in order
     * Pass onStart handler to HeroSection and FinalCTASection
     * Smooth scroll between sections

   - loading state:
     * Render LoadingScreen as overlay
     * Trigger model load on enter
     * onCancel: return to landing
     * onReady: go to chat state

   - chat state:
     * Render placeholder "Chat Interface"
     * "Ready to chat!" message

5. onStart handler:
   - Scroll to top or show loading immediately
   - Set appState to "loading"
   - Trigger Quick Mode download

6. Styling:
   - Full-page sections
   - Smooth scroll behavior
   - Consistent spacing between sections
</action>
<verify>
grep -q "HeroSection" src/App.tsx && \
grep -q "FinalCTASection" src/App.tsx && \
grep -q "LoadingScreen" src/App.tsx && \
grep -q "useSettingsStore" src/App.tsx && \
grep -q "landing" src/App.tsx && \
grep -q "loading" src/App.tsx
</verify>
<done>App.tsx integrated with full 11-section landing page and onboarding flow</done>
</task>

<task type="checkpoint:human-verify">
<what-built>
Complete marketing landing page with 11 sections:
- Hero with spark logo, gradient CTA, trust indicators
- Problem/Solution split screen
- Demo section showing offline capability
- How It Works (3 steps)
- Comparison table (Lokul vs ChatGPT)
- Use Cases (3 personas)
- Technical Trust (dark section with proof)
- FAQ accordion (6 questions)
- Testimonials (3 quotes)
- Final CTA (orange gradient)
- Footer (4 columns)
- Loading screen with download progress
- App.tsx integration with flow control
</what-built>
<how-to-verify>
1. Start development server: npm run dev
2. Open http://localhost:5173
3. Verify all 11 sections render correctly:
   - Hero: Spark logo pulsing, gradient CTA button
   - Problem/Solution: Split screen with pain points vs solutions
   - Demo: Visual showing offline capability
   - How It Works: 3 cards in a row
   - Comparison: Table with ChatGPT vs Lokul
   - Use Cases: 3 persona cards
   - Technical Trust: Dark background section
   - FAQ: Accordion that expands/collapses
   - Testimonials: 3 quote cards with stars
   - Final CTA: Orange gradient with white button
   - Footer: 4 columns with links
4. Test WebGPU error in Safari
5. Click "Start Chatting" - verify loading screen appears
6. Test cancel and return to landing
</how-to-verify>
<resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. All 11 landing page sections render correctly
2. HeroSection has spark logo with pulse animation and gradient CTA
3. ProblemSolutionSection has split-screen layout
4. All UI components (Button, Progress, Card) work correctly
5. LoadingScreen shows download progress with time estimates
6. WebGPU detection works
7. App.tsx integrates all sections with flow control
8. TypeScript compilation passes
9. Responsive design works on mobile
</verification>

<success_criteria>
- Hero section with spark logo, headline, gradient CTA within 2 seconds
- Problem/Solution split screen with pain points and solutions
- Demo section showing offline capability
- How It Works with 3 step cards
- Comparison table (Lokul vs ChatGPT)
- Use Cases with 3 persona cards
- Technical Trust dark section with proof points
- FAQ accordion with 6 questions
- Testimonials with 3 quote cards
- Final CTA orange gradient section
- Footer with 4 columns
- Loading screen with progress and cancel
- Onboarding flow works (landing → loading → chat)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-03-SUMMARY.md` documenting:
- 11-section landing page structure
- Component architecture
- Animation details (pulse, glow, scroll)
- Onboarding flow states
- Responsive breakpoints
</output>
