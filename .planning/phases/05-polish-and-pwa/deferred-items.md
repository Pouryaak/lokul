# Deferred Items

- `npm run lint` currently fails on pre-existing `max-lines-per-function` in `src/hooks/useMemory.ts:48` (`useMemory` has 210 lines; limit is 200). Out of scope for plan `05-05` mobile sidebar recursion fix.
